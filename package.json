{
  "name": "speakcai",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "英语口语教练 — React 19 + Express + 讯飞 ASR/TTS/ISE + DeepSeek",
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,green \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install:all": "cd server && npm i && cd ../client && npm i",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "test": "npm --prefix server run test",
    "lint": "npm --prefix server run lint && npm --prefix client run lint",
    "check": "npm run build && npm run test && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
